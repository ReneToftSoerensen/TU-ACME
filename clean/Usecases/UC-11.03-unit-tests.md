# UC-11.03: Unit Test Suite

## Narrative

As a **developer**, I want to **run unit tests that mock external dependencies**, so that **I can verify module functions in isolation without touching the file system or Posh-ACME store**.

## Acceptance Criteria

- [ ] Unit tests are tagged with `-Tag Unit` in Pester
- [ ] Unit tests mock Posh-ACME cmdlets (`New-PAAccount`, `Set-PAServer`, etc.)
- [ ] Unit tests mock file I/O (config.json read/write)
- [ ] Unit tests mock Windows Event Log operations
- [ ] Unit tests run in < 10 seconds
- [ ] `Invoke-Pester -Tag Unit` executes all unit tests
- [ ] All unit tests pass with green status
- [ ] New functions have corresponding unit tests

## Implementation Notes

- Test location: `tests/Unit/`
- Mocking via Pester's `Mock` and `InModuleScope`
- Fixtures available in `tests/Fixtures/`

## Test Coverage

**Unit:** All unit tests pass.

**Integration:** N/A (unit tests are isolated)

**Scripts:** N/A
