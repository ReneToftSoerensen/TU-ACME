# UC-11.04: Integration Test Suite

## Narrative

As a **developer**, I want to **run integration tests against a real Posh-ACME store**, so that **I can verify module wiring and data flow without mocking**.

## Acceptance Criteria

- [ ] Integration tests are tagged with `-Tag Integration` in Pester
- [ ] Integration tests exercise against real Posh-ACME store on disk (`%ProgramData%\Posh-ACME\`)
- [ ] Integration tests do not call mocked Posh-ACME cmdlets (real ones only)
- [ ] Integration tests create temporary config and account for testing
- [ ] Integration tests clean up after themselves (isolation)
- [ ] Integration tests run in < 30 seconds
- [ ] `Invoke-Pester -Tag Integration` executes all integration tests
- [ ] All integration tests pass with green status

## Implementation Notes

- Test location: `tests/Integration/`
- Fixtures available in `tests/Fixtures/`
- Tests set up temporary config and accounts
- Tests tear down after execution

## Test Coverage

**Unit:** N/A (integration tests use real objects)

**Integration:** All integration tests pass.

**Scripts:** N/A
