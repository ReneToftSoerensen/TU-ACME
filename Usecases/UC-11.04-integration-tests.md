# UC-11.04: Integration Test Suite

## Narrative

As a **developer**, I want to **run integration tests against a real Posh-ACME store**, so that **I can verify module wiring and data flow without mocking**.

## Acceptance Criteria

- [x] Integration tests are tagged with `-Tag Integration` in Pester
- [x] Integration tests exercise against real Posh-ACME store on disk (`%ProgramData%\Posh-ACME\`)
- [x] Integration tests do not call mocked Posh-ACME cmdlets (real ones only)
- [x] Integration tests create temporary config and account for testing
- [x] Integration tests clean up after themselves (isolation)
- [x] Integration tests run in < 30 seconds
- [x] `Invoke-Pester -Tag Integration` executes all integration tests
- [x] All integration tests pass with green status

## Implementation Notes

- Test location: `tests/Integration/`
- Fixtures available in `tests/Fixtures/`
- Tests set up temporary config and accounts
- Tests tear down after execution

## Test Coverage

**Unit:** N/A (integration tests use real objects)

**Integration:** All integration tests pass.

**Scripts:** N/A
